add different types of constraints from the complete one




getrundata("run")

json, 


getrundatatimestamp("","")


DA DIRE AL PROF:

per tenere in considerazione la componente perpendicolare all'elemento basta sommare alla global stiffness matrix dell'elemento un ulteriore stiffness matrix ma moltpiplicata per una translation (CS) matrix ortogonale?

aggiungere la forza distribuita perchè si simula la forza peso dell'elemento stesso, ma deve essere inserita ad un angolo? no non mi pare perchè portiamo la matrice di stiffness a globale

